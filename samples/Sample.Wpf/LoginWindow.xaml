<Window x:Class="Sample.Wpf.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="登录 - Cyaim 桌面示例"
        Width="420" Height="320"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.ColumnSpan="2" Text="Cyaim.Authentication 统一登录"
                   FontSize="18" FontWeight="Bold" Margin="0,0,0,16" />

        <TextBlock Grid.Row="1" Grid.Column="0" Text="服务器" VerticalAlignment="Center" Margin="0,4,12,4" />
        <TextBox x:Name="ServerBox" Grid.Row="1" Grid.Column="1" Margin="0,4"
                 Text="http://127.0.0.1:5299"
                 ToolTip="Sample.AuthServer 地址" />

        <TextBlock Grid.Row="2" Grid.Column="0" Text="用户名" VerticalAlignment="Center" Margin="0,4,12,4" />
        <TextBox x:Name="UserNameBox" Grid.Row="2" Grid.Column="1" Margin="0,4" />

        <TextBlock Grid.Row="3" Grid.Column="0" Text="密码" VerticalAlignment="Center" Margin="0,4,12,4" />
        <PasswordBox x:Name="PasswordBox" Grid.Row="3" Grid.Column="1" Margin="0,4"
                     KeyDown="PasswordBox_KeyDown" />

        <Button x:Name="LoginButton" Grid.Row="4" Grid.Column="1"
                Content="登 录" HorizontalAlignment="Right" MinWidth="100"
                Margin="0,12,0,0" IsDefault="True" Click="LoginButton_Click" />

        <TextBlock x:Name="ErrorText" Grid.Row="5" Grid.ColumnSpan="2"
                   Foreground="#C0392B" TextWrapping="Wrap" Margin="0,12,0,0" />
    </Grid>
</Window>
